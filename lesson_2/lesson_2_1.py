'''
------------------------------------------------------------------------------------------------------------------------
                                            Методы в классах
------------------------------------------------------------------------------------------------------------------------
'''
'''
------------------------------------------------------------------------------------------------------------------------
🔹 Как создавать методы класса
Метод — это обычная функция внутри класса
class User:
    def say_hi(self):
        print("Hi!")
------------------------------------------------------------------------------------------------------------------------
🔹 Вызов метода
u = User()
u.say_hi()  # Hi!
------------------------------------------------------------------------------------------------------------------------
🔹 Что такое self
👉 self — это ссылка на конкретный объект (экземпляр)

class User:
    def say_name(self):
        print(self.name)

u = User()
u.name = "Alex"

u.say_name()  # Alex
------------------------------------------------------------------------------------------------------------------------
🔹 Как это работает
u.say_name()
👉 на самом деле Python делает так:
User.say_name(u)
➡️ Поэтому нужен self — чтобы метод понимал, с каким объектом он работает
------------------------------------------------------------------------------------------------------------------------
🔹 Важные моменты
✔️ self — это просто имя (можно назвать иначе, но так не делают)
✔️ всегда первый параметр метода
✔️ через self получаем доступ к атрибутам:
self.name
self.age
------------------------------------------------------------------------------------------------------------------------
🔹 Мини-итог
Метод = функция внутри класса
self = текущий объект
Без self метод не знает, с кем работает
------------------------------------------------------------------------------------------------------------------------
'''
class Animal:
    color = 'green'
    paws = 4

    def set_color(self, color): # - функция принимает в себя self(текущий объект класса) и color
        self.color = color  # - устанавливает для текущего объекта класса color который был передан в функцию
        print('Установка цвета')

    def get_color(self): # - функция принимает в себя self(текущий объект класса)
        return self.color # - возвращает текущий_объект.цвет

animal = Animal()
# print(animal.color)
animal.set_color('blue')
# print(animal.color)
print(animal.get_color())
result = getattr(animal, 'get_color') # - при использовании getattr надо указывать название функции в кавычках
print(result()) # - при выводе надо ВЫЗЫВАТЬ то что пришло нам в функции выше, потому что там приходит просто self.color
"""
Объяснение того что выше про getattr:
------------------------------------------------------------------------------------------------------------------------
🔹 Что происходит в коде
result = getattr(animal, 'get_color')

🔹 1. Что возвращается в result
👉 В result возвращается сам метод (функция), привязанная к объекту
То есть:
result = animal.get_color
💡 Это НЕ результат работы метода, а ссылка на метод

🔹 2. Почему 'get_color' в кавычках
👉 Потому что getattr принимает имя атрибута как строку
getattr(obj, "имя_атрибута")
Если написать без кавычек:
getattr(animal, get_color)  # ❌ ошибка
➡️ Python будет искать переменную get_color, а не имя атрибута

3. Почему нужно писать result()
print(result())
👉 Потому что:
result → это метод (функция) - так как мы в него вернули сам метод (функцию)
чтобы выполнить функцию → нужны ()

🔹 4. Что происходит под капотом
result = getattr(animal, 'get_color')
➡️ это:
result = animal.get_color
➡️ а вызов:
result()
➡️ это:
animal.get_color()

🔹 5. Важный момент про self
Когда ты сделал:
result = animal.get_color
👉 метод уже привязан к объекту animal
➡️ self передастся автоматически
➡️ поэтому ты пишешь:
result()
а не:
result(animal)  # не нужно

🔹 Мини-итог (чтобы запомнить)
getattr(obj, "method") → возвращает метод (функцию)
имя всегда в "кавычках"
result → это НЕ результат, а функция
result() → вызывает функцию
self подставляется автоматически
"""

